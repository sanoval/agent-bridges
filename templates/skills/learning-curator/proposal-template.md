# Learning Proposal

<!--
Written by the learning-curator skill. Where it lives depends on Action:
  - append_memory  -> .agent-bridges/learning/pending/<slug>.md in the
                       TARGET PROJECT (gitignored, local working state).
  - patch_skill /
    create_skill   -> ~/.agent-bridges/learning/skills-pending/<slug>.md
                       in the USER'S HOME (shared across every project on
                       the machine — that's why Project is a field below).
See "Local staging and promote-on-approval" in SKILL.md for the full
workflow. Delete this file once its proposal is promoted or rejected; it
is not meant to accumulate.
-->

Project: `<name/path this proposal came from — always fill in for patch_skill/create_skill, since the pending directory is shared across projects>`
Target: `<~/.agent-bridges/skills/<name>/SKILL.md or AGENTS.md>`
Action: `<append_memory | patch_skill | create_skill>`
Score: `<0-10>`
Task: `<one-line task summary>`

## Evidence

- `<evidence item 1, with source: user / test / review / qa / security / repo-doc / inference>`
- `<evidence item 2>`

## Proposed change

`<the exact text to add/patch, ready to paste into the target file
verbatim on approval — not a paraphrase the human has to reconstruct>`

## Reason

`<why this is reusable and why the evidence above supports it>`

## Reviewer decision

`<left blank by the curator; filled in by the human reviewer>`

- [ ] Approved — promoted to `<target file>`, logged in `<.agent-bridges/learning/log.md (memory) or ~/.agent-bridges/learning/skills-log.md (skill)>`
- [ ] Rejected — reason: `<...>`
